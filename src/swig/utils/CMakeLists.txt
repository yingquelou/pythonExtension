if(WIN32)
    cmake_path(GET CMAKE_CURRENT_LIST_DIR STEM PROJ)
    set(CMAKE_INCLUDE_CURRENT_DIR ON)
    swig_python_library(${PROJ} SOURCE utils.i)
    target_compile_definitions(${PROJ} PRIVATE UNICODE)
    swig_test()
endif()